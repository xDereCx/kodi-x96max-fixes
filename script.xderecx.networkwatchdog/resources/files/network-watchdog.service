[Unit]
Description=Network watchdog - restart connman if no LAN IP shows up at boot
After=network.target

[Service]
Type=oneshot
ExecStart=/storage/.config/network-watchdog.sh
RemainAfterExit=no

[Install]
WantedBy=multi-user.target
